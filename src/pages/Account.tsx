export default function Account() {
  return (
    <div className="mx-auto max-w-4xl px-6 py-12">
      <p className="text-xs uppercase tracking-[0.3em] text-neon-500">Compte</p>
      <h1 className="mt-2 font-display text-3xl">Mon compte</h1>
      <p className="mt-4 text-sm text-ink-600">
        Espace client mock. Cette page servira de hub (profil, adresses, commandes) une fois
        connecté au backend.
      </p>
    </div>
  )
}
